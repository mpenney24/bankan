services:
    backend:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: bankan-backend
        env_file:
            - .env
        ports:
            - '5173:5173'
        environment:
            - PORT=5173
        restart: unless-stopped
